"use client";
// Корневая /tg — лучше всего пустая, т.к. layout сам редиректит на /tg/dashboard
// Если попали сюда — показываем загрузочный экран до того как layout сработает.
export default function TgIndexPage() {
  return null;
}
